# Database (local dev only — not used in checkout flow)
DATABASE_URL=file:/home/z/my-project/db/custom.db

# XPayments Gateway V2 — S2S Contract
# XPAYMENTS_API_KEY is the ONLY required var for checkout
XPAYMENTS_API_KEY="sk_live_..."
# storeId is OPTIONAL — if missing/empty, it will be omitted from the request
XPAYMENTS_STORE_ID="b2816d52-5e54-4942-b301-2a2c42c2093d"

# NeXFlowX (legacy — not active)
NEXFLOWX_API_KEY=""
NEXFLOWX_WEBHOOK_SECRET=""